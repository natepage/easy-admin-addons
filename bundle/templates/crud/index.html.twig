{% extends '@EasyAdmin/crud/index.html.twig' %}

{% block main %}
    {% set crudAddons = adminAddonsContext().crudAddons %}

    {% if crudAddons.renderTablesInCard %}
        <div class="card">
            <div class="card-body">
                {{ parent() }}
            </div>
        </div>
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock main %}
