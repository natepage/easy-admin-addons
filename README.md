# natepage/easy-admin-addons

Sets of additional functionalities for EasyCorp/EasyAdminBundle

## TODO

- [X] Custom theme (CSS)
- [X] Timezone management (resolver, form type, etc.)
- [X] Base abstract controllers with sensible defaults
- [X] Support for AWS DynamoDB instead of Doctrine ORM (ideally per CRUD so that it can be used in the same project as the Doctrine ORM)
- [ ] Improve class metadata in DynamoDB implementation as EasyAdminBundle relies on it for some features (e.g. BackedEnum support in ChoiceField)
